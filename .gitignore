/target
**/*.rs.bk
*.pdb

# Tooling
node_modules/
.next/
dist/
.turbo/

# Local data
*.sqlite
*.sqlite-journal
*.db
/data/
/logs/
.env
.env.local

# The generated config. It holds plaintext passwords and one operator's network
# layout, neither of which belongs to anyone else. The binary writes a default
# on first run; `openshard_config::DEFAULT_TOML` is the version under review.
/openshard.toml

# Editors / OS
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json

# Local agent tooling: notes for the assistant and an MCP server config that
# points at one machine's absolute binary path. Neither is anyone else's.
CLAUDE.md
.mcp.json
# Embedding models the code-index server downloads into the working directory.
.fastembed_cache/
.skeleton/
